# my-study-log

个人学习与生活时间记录（纯前端，数据存在浏览器本地）。

在线使用：<https://dooorr.github.io/my-study-log/>

## 功能

### 计时

- 普通正计时、番茄钟（默认 25+5 分钟，可改）
- 科目：数分、高代、英语、政治；生活类：睡觉、玩手机；可自定义类别
- 子任务（如「4.2」），预设库 + 计时时一键点选
- 圆环进度、完成动效；番茄专注结束自动记入学习时长

### 今日

- **倒数日**（默认考研初试，可在「更多」里改日期和名称）
- 每日学习目标（默认 8h，仅统计学习类科目）与顶部进度条
- 记睡眠、手动补录
- 今日记录列表：**拖左侧 ⋮⋮ 可调整显示顺序**（不影响时长统计）
- 连续打卡天数

### 统计

- 学习月历（点某天看详情）
- 近 7 天柱状图、30 天科目分布、子任务统计
- 时间占比饼图（7 天 / 30 天）
- 导出 ICS 导入系统日历（需手动重新导出，非实时同步）

### 更多

- 深色 / 浅色模式（右上角 🌙 / ☀️，跟随系统首访）
- 自定义类别、子任务预设
- JSON 导出 / 导入备份；**把备份 JSON 拖到页面上也可导入**
- 添加到主屏幕说明（Safari / Chrome）

### 手机

- 底栏：计时 / 今日 / 统计 / 更多
- 建议 Safari 打开并「添加到主屏幕」当 App 用

## 数据与备份

- 打卡数据**不会上传到 GitHub**，只存在本机浏览器 `localStorage`
- 清缓存、换浏览器会丢数据，请定期「更多 → 导出 JSON」
- 电脑与手机数据**不自动同步**，换设备用导出 / 导入
- 备份 JSON 当前字段：`logs`、`customCategories`、`subtasksLibrary`、`dailyGoalHours`、`pomodoro`、`timerMode`、`dayLogOrder`、`countdown`（可选）

## 本地运行

双击 `index.html`，或在本目录执行：

```bash
py -m http.server 8080
```

浏览器打开 `http://127.0.0.1:8080`。手机同 WiFi 访问需用 `start-server.bat`（见文件夹内 `使用说明.txt`，本地脚本不必推仓库）。

## 文件

| 文件 | 说明 |
|------|------|
| `index.html` | 页面结构 |
| `style.css` | 样式与深色模式 |
| `app.js` | 逻辑与本地存储 |

## 许可证

个人学习使用。打卡数据归使用者本人。
